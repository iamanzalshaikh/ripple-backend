# Disabled placeholder — wire your host, secrets, and deploy steps when ready.
name: Deploy (template)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Configure deploy steps for ripple-backend"
