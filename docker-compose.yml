services:
  ripple-backend:
    build: .
    container_name: ripple-backend
    ports:
      - "3001:3001"
    environment:
      NODE_ENV: development
      PORT: 3001
      FRONTEND_URL: http://localhost:8081
